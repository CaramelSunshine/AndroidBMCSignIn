<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projectindigo.case_study_android_apps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.Splash"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".UI.Home_Screen" />
        <activity android:name=".UI.SignUp" />
        <activity android:name=".UI.Reason_For_Visit" />
        <activity android:name=".PasswordDialog" android:theme="@style/DialogTheme"></activity>
    </application>

</manifest>